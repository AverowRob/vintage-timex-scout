# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Env / secrets
.env

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# OS
.DS_Store

# Runtime state — kept local, EXCEPT the demo pull snapshot the deployed app loads
# so it shows real enriched listings without live API calls (galleries, specifics, age).
state/*
!state/last_pull.json
*.local.json

# Editor / agent tooling config (machine-specific)
.claude/

# Personal presentation deck — kept local, not part of the submission
presentation.html
